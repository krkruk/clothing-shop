[project]
name = "clothingshop-acceptance-tests"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0",
    "pytest-playwright>=0.5",
    "playwright>=1.40",
    "requests>=2.31",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
base_url = "http://localhost:8080"
